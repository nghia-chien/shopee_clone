export async function authenticateUser(email: string, _password: string) {
	return { id: '1', email };
}

export async function registerUser(email: string, _password: string) {
	return { id: '1', email };
}
